import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "LifeOrganizer | Your All-in-One Productivity Solution",
  description:
    "Manage tasks, habits, goals, journals, and projects all in one place with LifeOrganizer, the ultimate productivity tool for your life.",
  keywords:
    "productivity, task management, habit tracker, goal setting, journal, project management",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
